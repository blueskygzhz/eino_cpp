╔══════════════════════════════════════════════════════════════════════════════╗
║             OnEndWithStreamOutput 实现细节 - 快速参考                          ║
╚══════════════════════════════════════════════════════════════════════════════╝

📍 位置: eino_cpp/include/eino/compose/utils.h:92-107
🎯 作用: 在流式输出完成后触发 callback handlers

────────────────────────────────────────────────────────────────────────────────
📝 函数签名
────────────────────────────────────────────────────────────────────────────────

template <typename T>
std::pair<Context, std::shared_ptr<StreamReader<T>>> OnEndWithStreamOutput(
    const Context& ctx,
    std::shared_ptr<StreamReader<T>> output);

────────────────────────────────────────────────────────────────────────────────
🔄 执行流程
────────────────────────────────────────────────────────────────────────────────

1. 从 Context 获取所有 handlers
   └─ callbacks::GetHandlersFromContext(ctx)

2. 遍历每个 handler,依次调用
   └─ handler->OnEndWithStreamOutput(new_ctx, output)

3. 返回更新后的 context 和原始 stream
   └─ {updated_ctx, original_output}

────────────────────────────────────────────────────────────────────────────────
🎯 调用时机
────────────────────────────────────────────────────────────────────────────────

✅ Stream() 方法返回后:
   OnStart(input) → stream_func(...) → OnEndWithStreamOutput(output_stream)

✅ Transform() 方法返回后:
   OnStartWithStreamInput(input_stream) → transform_func(...) 
   → OnEndWithStreamOutput(output_stream)

❌ Invoke() 方法不会调用 (使用 OnEnd 代替)
❌ Collect() 方法不会调用 (使用 OnEnd 代替)

────────────────────────────────────────────────────────────────────────────────
🔑 关键特性
────────────────────────────────────────────────────────────────────────────────

✅ 遍历所有 handlers
✅ 更新 Context (通过返回值)
✅ 传递 StreamReader 引用

⚠️  当前限制: 所有 handlers 共享同一个 stream
   - 如果某个 handler 读取了 stream,后续 handler 无法读取
   - Go 版本有 Stream Copy 机制,C++ 版本待实现

────────────────────────────────────────────────────────────────────────────────
⚠️  重要注意事项
────────────────────────────────────────────────────────────────────────────────

1. ❌ 不要在 handler 中消费 stream
   ```cpp
   // 错误示例
   Context OnEndWithStreamOutput(ctx, output) {
       T value;
       while (output->Read(value)) { ... }  // ❌ 消费了 stream!
       return ctx;
   }
   ```

2. ✅ 只记录事件或传递引用
   ```cpp
   // 正确示例
   Context OnEndWithStreamOutput(ctx, output) {
       LogEvent("Stream completed");        // ✅ 只记录
       event_sender_->SendStream(output);   // ✅ 传递引用
       return ctx;
   }
   ```

3. ✅ 正确更新 Context
   ```cpp
   Context OnEndWithStreamOutput(ctx, output) {
       Context new_ctx = ctx;
       new_ctx.SetValue("completed", true);
       return new_ctx;  // ✅ 返回更新后的 context
   }
   ```

────────────────────────────────────────────────────────────────────────────────
📊 与其他 Callback 的对比
────────────────────────────────────────────────────────────────────────────────

OnStart<T>                 : 非流式输入开始前
OnEnd<T>                   : 非流式输出结束后
OnStartWithStreamInput<T>  : 流式输入开始前
OnEndWithStreamOutput<T>   : 流式输出结束后  ← 我们在这里
OnError                    : 执行出错时

配对使用:
- Stream   : OnStart + OnEndWithStreamOutput
- Collect  : OnStartWithStreamInput + OnEnd
- Transform: OnStartWithStreamInput + OnEndWithStreamOutput

────────────────────────────────────────────────────────────────────────────────
🚀 使用示例
────────────────────────────────────────────────────────────────────────────────

// 1. 定义 Handler
class MyHandler : public callbacks::Handler {
public:
    Context OnEndWithStreamOutput(const Context& ctx, auto output) override {
        std::cout << "Stream completed!" << std::endl;
        return ctx;
    }
};

// 2. 设置到 Context
auto handler = std::make_shared<MyHandler>();
ctx = callbacks::AppendHandlers(ctx, info, {handler});

// 3. 调用 Stream 方法
auto stream = runnable->Stream(ctx, "input", {});
// 内部会自动调用: OnEndWithStreamOutput(ctx, stream)

────────────────────────────────────────────────────────────────────────────────
🔍 Go vs C++ 关键差异
────────────────────────────────────────────────────────────────────────────────

特性               Go 版本                    C++ 版本
─────────────────  ────────────────────────  ────────────────────────
Stream 复制        ✅ output.Copy()          ❌ 暂未实现
Handler 独立性     ✅ 每个 handler 独立副本  ⚠️  共享同一个 stream
类型转换           ✅ StreamReaderWithConvert ⚠️  直接传递

────────────────────────────────────────────────────────────────────────────────
📚 详细文档
────────────────────────────────────────────────────────────────────────────────

完整文档: docs/OnEndWithStreamOutput_IMPLEMENTATION.md
包含内容:
  - 详细流程图
  - Go vs C++ 对比
  - Stream Copy 机制说明
  - 完整使用示例
  - 最佳实践
  - 未来改进方向

────────────────────────────────────────────────────────────────────────────────
